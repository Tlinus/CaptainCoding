<h1> Créer un Chat </h1>

<%= form_for :cat, url: cats_path do |f| %>
	<p>
		<%= f.label :nom %><br>
		<%= f.text_field :nom %> 
	</p>

	<p>
		<%= f.label :surnom %><br>
		<%= f.text_field :surnom %> 
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>